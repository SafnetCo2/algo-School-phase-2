let greet;
greet = 'Hello World';
greet = 'Enyy';
console.log(greet);

//output enny why When the value is changed, the old data is removed from the variable

let $ = 1; // declared a variable with the name "$"
let _ = 2; // and now a variable with the name "_"

console.log($ + _); // 3

let admin, name; //can declare two variables once

name = 'john';
admin = name;
console.log(admin);


// let n = 'kelly';
// n = 1234;
// console.log(n);
// typeof (n);

//logic
let hour = 9;
if (hour < 9 || hour < 10) {
    console.log('office clost')
}