const message = 'javascript its fun';
let result = message.includes('tis');
console.log(result);
//The includes() method checks if one string can be found inside another string.

const song = ' I know my redeemer lives ';
let songIncludes = song.includes('redeemer');
console.log(songIncludes);