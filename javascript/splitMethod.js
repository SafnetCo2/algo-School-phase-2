//The split() method divides a string into a list of substrings and returns them as an array.
const message = 'javascript ::is ::fun';
//devides the message string at ::
let res = message.split('::');
console.log(res);

// Output: [ 'JavaScript', 'is', 'fun' ]

